env:
  LANG: "en_GB.UTF-8"

steps:

  - label: 'Skip demo'
    timeout_in_minutes: 10
    agents:
      queue: macos
    commands:
      - bundle install
      - bundle exec maze-runner features/skip.feature
