env:
  LANG: "en_GB.UTF-8"

steps:
  ##############################################################################
  #
  # Build
  #

  - label: Static framework and Swift Package Manager builds
    key: framework_build
    timeout_in_minutes: 10
    agents:
      queue: macos-15
    env:
      XCODE_VERSION: 16.3.0
    commands:
      - make build_xcframework
      - make build_swift
      - make build_ios_static
      - zip -ry Bugsnag.xcframework.zip build/xcframeworks/products/Bugsnag.xcframework
      - zip -ry BugsnagNetworkRequestPlugin.xcframework.zip build/xcframeworks/products/BugsnagNetworkRequestPlugin.xcframework
    plugins:
      - artifacts#v1.9.4:
          upload:
            - "Bugsnag.xcframework.zip"
            - "BugsnagNetworkRequestPlugin.xcframework.zip"

  - label: Build test fixtures
    key: cocoa_fixture
    timeout_in_minutes: 15
    agents:
      queue: macos-15
    env:
      XCODE_VERSION: 16.3.0
    artifact_paths:
      - features/fixtures/ios/output/iOSTestApp_Release.ipa
      - features/fixtures/ios/output/iOSTestApp_Debug.ipa
      - features/fixtures/macos/output/macOSTestApp_Release.zip
      - features/fixtures/macos/output/macOSTestApp_Debug.zip
      - features/fixtures/ios/output/ipa_url_bb_release.txt
      - features/fixtures/ios/output/ipa_url_bs_release.txt
      - features/fixtures/ios/output/ipa_url_bb_debug.txt
      - features/fixtures/ios/output/ipa_url_bs_debug.txt
    commands:
      - ruby scripts/build-test-fixture.rb

  - label: Carthage
    timeout_in_minutes: 15
    agents:
      queue: macos-15
    commands:
      - ./scripts/build-carthage.sh
    plugins:
      artifacts#v1.9.4:
        upload: "features/fixtures/carthage/carthage-*.log"

  

  

  ##############################################################################
  #
  # Full set of E2E tests on one iOS version
  #

  #
  # BrowserStack
  #
  - label: ':browserstack: iOS 18 State breadcrumbs v1'
    depends_on:
      - cocoa_fixture
    timeout_in_minutes: 60
    agents:
      queue: opensource
    plugins:
      artifacts#v1.9.4:
        download: "features/fixtures/ios/output/ipa_url_bs_release.txt"
        upload:
          - "maze_output/failed/**/*"
          - "maze_output/maze_output.zip"
      docker-compose#v5.8.0:
        pull: cocoa-maze-runner
        run: cocoa-maze-runner
        service-ports: true
        command:
          - "--app=@/app/build/ipa_url_bs_release.txt"
          - "--farm=bs"
          - "--device=IOS_18"
          - "--appium-version=1.21.0"
          - "--a11y-locator"
          - "features/release/breadcrumbs_2.feature"
      test-collector#v1.11.0:
        files: "reports/TEST-*.xml"
        format: "junit"
        branch: "^master|next$$"
    concurrency: 5
    concurrency_group: 'browserstack-app'
    concurrency_method: eager
    retry:
      automatic:
        - exit_status: -1  # Agent was lost
          limit: 2
        - exit_status: 103  # Appium session failed
          limit: 2
          
          
  - label: ':browserstack: iOS 18 State breadcrumbs v3'
    depends_on:
      - cocoa_fixture
    timeout_in_minutes: 60
    agents:
      queue: opensource
    plugins:
      artifacts#v1.9.4:
        download: "features/fixtures/ios/output/ipa_url_bs_release.txt"
        upload:
          - "maze_output/failed/**/*"
          - "maze_output/maze_output.zip"
      docker-compose#v5.8.0:
        pull: cocoa-maze-runner
        run: cocoa-maze-runner
        service-ports: true
        command:
          - "--app=@/app/build/ipa_url_bs_release.txt"
          - "--farm=bs"
          - "--device=IOS_18"
          - "--appium-version=1.21.0"
          - "--a11y-locator"
          - "features/release/breadcrumbs_2.feature"
      test-collector#v1.11.0:
        files: "reports/TEST-*.xml"
        format: "junit"
        branch: "^master|next$$"
    concurrency: 5
    concurrency_group: 'browserstack-app'
    concurrency_method: eager
    retry:
      automatic:
        - exit_status: -1  # Agent was lost
          limit: 2
        - exit_status: 103  # Appium session failed
          limit: 2
          
          
  - label: ':browserstack: iOS 18 State breadcrumbs v4'
    depends_on:
      - cocoa_fixture
    timeout_in_minutes: 60
    agents:
      queue: opensource
    plugins:
      artifacts#v1.9.4:
        download: "features/fixtures/ios/output/ipa_url_bs_release.txt"
        upload:
          - "maze_output/failed/**/*"
          - "maze_output/maze_output.zip"
      docker-compose#v5.8.0:
        pull: cocoa-maze-runner
        run: cocoa-maze-runner
        service-ports: true
        command:
          - "--app=@/app/build/ipa_url_bs_release.txt"
          - "--farm=bs"
          - "--device=IOS_18"
          - "--appium-version=1.21.0"
          - "--a11y-locator"
          - "features/release/breadcrumbs_2.feature"
      test-collector#v1.11.0:
        files: "reports/TEST-*.xml"
        format: "junit"
        branch: "^master|next$$"
    concurrency: 5
    concurrency_group: 'browserstack-app'
    concurrency_method: eager
    retry:
      automatic:
        - exit_status: -1  # Agent was lost
          limit: 2
        - exit_status: 103  # Appium session failed
          limit: 2
          
          
  - label: ':browserstack: iOS 18 State breadcrumbs v5'
    depends_on:
      - cocoa_fixture
    timeout_in_minutes: 60
    agents:
      queue: opensource
    plugins:
      artifacts#v1.9.4:
        download: "features/fixtures/ios/output/ipa_url_bs_release.txt"
        upload:
          - "maze_output/failed/**/*"
          - "maze_output/maze_output.zip"
      docker-compose#v5.8.0:
        pull: cocoa-maze-runner
        run: cocoa-maze-runner
        service-ports: true
        command:
          - "--app=@/app/build/ipa_url_bs_release.txt"
          - "--farm=bs"
          - "--device=IOS_18"
          - "--appium-version=1.21.0"
          - "--a11y-locator"
          - "features/release/breadcrumbs_2.feature"
      test-collector#v1.11.0:
        files: "reports/TEST-*.xml"
        format: "junit"
        branch: "^master|next$$"
    concurrency: 5
    concurrency_group: 'browserstack-app'
    concurrency_method: eager
    retry:
      automatic:
        - exit_status: -1  # Agent was lost
          limit: 2
        - exit_status: 103  # Appium session failed
          limit: 2
          
          
  - label: ':browserstack: iOS 18 State breadcrumbs v6'
    depends_on:
      - cocoa_fixture
    timeout_in_minutes: 60
    agents:
      queue: opensource
    plugins:
      artifacts#v1.9.4:
        download: "features/fixtures/ios/output/ipa_url_bs_release.txt"
        upload:
          - "maze_output/failed/**/*"
          - "maze_output/maze_output.zip"
      docker-compose#v5.8.0:
        pull: cocoa-maze-runner
        run: cocoa-maze-runner
        service-ports: true
        command:
          - "--app=@/app/build/ipa_url_bs_release.txt"
          - "--farm=bs"
          - "--device=IOS_18"
          - "--appium-version=1.21.0"
          - "--a11y-locator"
          - "features/release/breadcrumbs_2.feature"
      test-collector#v1.11.0:
        files: "reports/TEST-*.xml"
        format: "junit"
        branch: "^master|next$$"
    concurrency: 5
    concurrency_group: 'browserstack-app'
    concurrency_method: eager
    retry:
      automatic:
        - exit_status: -1  # Agent was lost
          limit: 2
        - exit_status: 103  # Appium session failed
          limit: 2
          
          
  - label: ':browserstack: iOS 18 State breadcrumbs v7'
    depends_on:
      - cocoa_fixture
    timeout_in_minutes: 60
    agents:
      queue: opensource
    plugins:
      artifacts#v1.9.4:
        download: "features/fixtures/ios/output/ipa_url_bs_release.txt"
        upload:
          - "maze_output/failed/**/*"
          - "maze_output/maze_output.zip"
      docker-compose#v5.8.0:
        pull: cocoa-maze-runner
        run: cocoa-maze-runner
        service-ports: true
        command:
          - "--app=@/app/build/ipa_url_bs_release.txt"
          - "--farm=bs"
          - "--device=IOS_18"
          - "--appium-version=1.21.0"
          - "--a11y-locator"
          - "features/release/breadcrumbs_2.feature"
      test-collector#v1.11.0:
        files: "reports/TEST-*.xml"
        format: "junit"
        branch: "^master|next$$"
    concurrency: 5
    concurrency_group: 'browserstack-app'
    concurrency_method: eager
    retry:
      automatic:
        - exit_status: -1  # Agent was lost
          limit: 2
        - exit_status: 103  # Appium session failed
          limit: 2
          
          
  - label: ':browserstack: iOS 18 State breadcrumbs v8'
    depends_on:
      - cocoa_fixture
    timeout_in_minutes: 60
    agents:
      queue: opensource
    plugins:
      artifacts#v1.9.4:
        download: "features/fixtures/ios/output/ipa_url_bs_release.txt"
        upload:
          - "maze_output/failed/**/*"
          - "maze_output/maze_output.zip"
      docker-compose#v5.8.0:
        pull: cocoa-maze-runner
        run: cocoa-maze-runner
        service-ports: true
        command:
          - "--app=@/app/build/ipa_url_bs_release.txt"
          - "--farm=bs"
          - "--device=IOS_18"
          - "--appium-version=1.21.0"
          - "--a11y-locator"
          - "features/release/breadcrumbs_2.feature"
      test-collector#v1.11.0:
        files: "reports/TEST-*.xml"
        format: "junit"
        branch: "^master|next$$"
    concurrency: 5
    concurrency_group: 'browserstack-app'
    concurrency_method: eager
    retry:
      automatic:
        - exit_status: -1  # Agent was lost
          limit: 2
        - exit_status: 103  # Appium session failed
          limit: 2
          
          
  - label: ':browserstack: iOS 18 State breadcrumbs v9'
    depends_on:
      - cocoa_fixture
    timeout_in_minutes: 60
    agents:
      queue: opensource
    plugins:
      artifacts#v1.9.4:
        download: "features/fixtures/ios/output/ipa_url_bs_release.txt"
        upload:
          - "maze_output/failed/**/*"
          - "maze_output/maze_output.zip"
      docker-compose#v5.8.0:
        pull: cocoa-maze-runner
        run: cocoa-maze-runner
        service-ports: true
        command:
          - "--app=@/app/build/ipa_url_bs_release.txt"
          - "--farm=bs"
          - "--device=IOS_18"
          - "--appium-version=1.21.0"
          - "--a11y-locator"
          - "features/release/breadcrumbs_2.feature"
      test-collector#v1.11.0:
        files: "reports/TEST-*.xml"
        format: "junit"
        branch: "^master|next$$"
    concurrency: 5
    concurrency_group: 'browserstack-app'
    concurrency_method: eager
    retry:
      automatic:
        - exit_status: -1  # Agent was lost
          limit: 2
        - exit_status: 103  # Appium session failed
          limit: 2
          
          
  - label: ':browserstack: iOS 18 State breadcrumbs v10'
    depends_on:
      - cocoa_fixture
    timeout_in_minutes: 60
    agents:
      queue: opensource
    plugins:
      artifacts#v1.9.4:
        download: "features/fixtures/ios/output/ipa_url_bs_release.txt"
        upload:
          - "maze_output/failed/**/*"
          - "maze_output/maze_output.zip"
      docker-compose#v5.8.0:
        pull: cocoa-maze-runner
        run: cocoa-maze-runner
        service-ports: true
        command:
          - "--app=@/app/build/ipa_url_bs_release.txt"
          - "--farm=bs"
          - "--device=IOS_18"
          - "--appium-version=1.21.0"
          - "--a11y-locator"
          - "features/release/breadcrumbs_2.feature"
      test-collector#v1.11.0:
        files: "reports/TEST-*.xml"
        format: "junit"
        branch: "^master|next$$"
    concurrency: 5
    concurrency_group: 'browserstack-app'
    concurrency_method: eager
    retry:
      automatic:
        - exit_status: -1  # Agent was lost
          limit: 2
        - exit_status: 103  # Appium session failed
          limit: 2
